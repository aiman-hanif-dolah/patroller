## 2024-07-06 - Tooltips for Accessibility

**Learning:** When using `IconButton` in Flutter without a text label, it lacks a semantic label for screen readers and contextual help for desktop users. Providing a `tooltip` property solves both issues simultaneously.
**Action:** Always add tooltips to icon-only buttons to ensure they are accessible and intuitive.
