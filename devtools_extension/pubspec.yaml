name: patroller_devtools_extension
description: Patroller DevTools extension panel.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  devtools_extensions: ^0.5.1
  flutter:
    sdk: flutter
  http: ^1.2.2
  web_socket_channel: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
